query getUsers {
  getUsers {
    userId
    createdAt
    updatedAt
  }
}

query getMyUser {
  getMyUser {
    userId
    email
    firstName
    lastName
    profilePictureUrl
    profiles {
      profileType
      program {
        programId
        name
        iconUrl
        slug
      }
    }
  }
}
