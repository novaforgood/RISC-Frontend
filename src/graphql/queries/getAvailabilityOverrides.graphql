query getAvailabilityOverrides($profileId: String!) {
  getAvailabilityOverrides(profileId: $profileId) {
    availabilityOverrideId
    profileId
    date
    availabilityOverrideIntervals {
      startTime
      endTime
    }
  }
}
