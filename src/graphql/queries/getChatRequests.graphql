query getChatRequests($profileId: String!) {
  getChatRequests(profileId: $profileId) {
    chatRequestId
    chatRequestStatus
    chatStartTime
    chatEndTime
    chatRequestMessage
    chatRejectMessage
    mentorProfileId
    mentorProfile {
      user {
        userId
        firstName
        lastName
        profilePictureUrl
      }
    }
    menteeProfileId
    menteeProfile {
      profileJson
      tagsJson
      bio
      user {
        email
        firstName
        lastName
        profilePictureUrl
      }
      profileTags {
        profileTagId
        name
      }
    }
  }
}
