query getApplications($programId: String!, $profileType: String!) {
  getApplications(programId: $programId, profileType: $profileType) {
    applicationJson
    applicationStatus
    createdAt
    profile {
      profileId
      userId
      user {
        firstName
        lastName
      }
    }
  }
}
