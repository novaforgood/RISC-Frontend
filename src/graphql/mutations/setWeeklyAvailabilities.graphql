mutation setWeeklyAvailabilities(
  $profileId: String!
  $availabilities: [DateInterval!]!
) {
  setWeeklyAvailabilities(
    profileId: $profileId
    availabilities: $availabilities
  ) {
    profileId
  }
}
