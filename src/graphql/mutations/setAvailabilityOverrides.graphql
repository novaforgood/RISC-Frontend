mutation setAvailabilityOverrides(
  $availabilityOverrideId: String!
  $availabilities: [DateInterval!]!
) {
  setAvailabilityOverrides(
    availabilityOverrideId: $availabilityOverrideId
    availabilities: $availabilities
  ) {
    profileId
  }
}
