mutation rejectApplication($applicationId: String!) {
  rejectApplication(applicationId: $applicationId) {
    userId
    applicationType
    applicationStatus
    applicationJson
    createdAt
  }
}
