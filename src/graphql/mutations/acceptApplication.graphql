mutation acceptApplication($applicationId: String!) {
  acceptApplication(applicationId: $applicationId) {
    userId
    applicationType
    applicationStatus
    applicationJson
    createdAt
  }
}
