mutation rejectChatRequest(
  $chatRequestId: String!
  $chatRejectMessage: String!
) {
  rejectChatRequest(
    chatRequestId: $chatRequestId
    chatRejectMessage: $chatRejectMessage
  ) {
    chatRequestId
  }
}
