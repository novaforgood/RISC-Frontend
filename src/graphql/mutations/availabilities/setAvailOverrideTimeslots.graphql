mutation setAvailOverrideTimeslots(
  $profileId: String!
  $availabilities: [DateInterval!]!
) {
  setAvailOverrideTimeslots(
    profileId: $profileId
    availabilities: $availabilities
  ) {
    profileId
  }
}
