mutation setAvailOverrideDates(
  $profileId: String!
  $availabilities: [DateInterval!]!
) {
  setAvailOverrideDates(
    profileId: $profileId
    availabilities: $availabilities
  ) {
    profileId
  }
}
